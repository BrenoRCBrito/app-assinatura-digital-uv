import React from 'react';
import { View, type ViewProps } from 'react-native';

function MapView(props: ViewProps) {
  return <View {...props} />;
}

export function Marker(props: ViewProps) {
  return <View {...props} />;
}

export default MapView;
